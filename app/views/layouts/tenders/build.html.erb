<!DOCTYPE html>
<html lang="en">

<% content_for :title do %>
  <%= step.to_s.humanize %>/<%= @tender.project_name %> - TenderSwift
<% end %>

<%= render 'layouts/head' %>

<body class="<%= controller_name %> <%= action_name %>">

<%= render 'contractors/navbar' %>

<%= render 'layouts/flash_messages' %>

<div class="content pb-5">

  <nav class="navbar navbar-light bg-light border-bottom justify-content-center
              d-flex d-sm-flex d-md-flex d-lg-none d-xl-none">
    <div class="navbar-text">
      <%= step.to_s.humanize %>
    </div>
  </nav>

  <div class="w-100 bg-light text-dark nav-tabs
              d-none d-sm-none d-md-none d-lg-block d-xl-block">
    <div class="container pt-3">
      <div class="row">
        <div class="col-lg-9 mx-auto">
          <nav aria-label="breadcrumb" role="navigation">
            <ol class="breadcrumb top-nav-breadcrumb">
              <li class="mt-1 mr-2">
                <svg aria-hidden="true" style="color: #959da5 !important;" height="16" version="1.1" viewBox="0 0 12 16" width="12">
                  <path fill-rule="evenodd" d="M4 9H3V8h1v1zm0-3H3v1h1V6zm0-2H3v1h1V4zm0-2H3v1h1V2zm8-1v12c0 .55-.45 1-1 1H6v2l-1.5-1.5L3 16v-2H1c-.55 0-1-.45-1-1V1c0-.55.45-1 1-1h10c.55 0 1 .45 1 1zm-1 10H1v2h2v-1h3v1h5v-2zm0-10H2v9h9V1z"></path>
                </svg>
              </li>
              <li class="breadcrumb-item">
                <a href="<%= contractor_root_path %>">
                  Home
                </a>
              </li>
              <li class="breadcrumb-item text-truncate"
                  style="max-width: 420px;">
                <a href="<%= tender_build_path(
                                 @tender, :general_information
                             ) %>">
                  <%= @tender.project_name %>
                </a>
              </li>
              <li class="breadcrumb-item active" aria-current="page">
                <%= step.to_s.humanize %>
              </li>
            </ol>
          </nav>
        </div>
      </div>
    </div>
  </div>

  <%= yield %>

</div>

</body>
</html>