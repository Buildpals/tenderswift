<%= form_with(model: boq, local: true) do |form| %>
  <% if boq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boq.errors.count, "error") %> prohibited this boq from being saved:</h2>

      <ul>
      <% boq.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :boq_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
