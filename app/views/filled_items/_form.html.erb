<%= form_with(model: filled_item, local: true) do |form| %>
  <% if filled_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filled_item.errors.count, "error") %> prohibited this filled_item from being saved:</h2>

      <ul>
      <% filled_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :filled_item_amount %>
  </div>

  <div class="field">
    <%= form.label :rate %>
    <%= form.text_field :rate, id: :filled_item_rate %>
  </div>

  <div class="field">
    <%= form.label :participant_id %>
    <%= form.text_field :participant_id, id: :filled_item_participant_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :filled_item_item_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
