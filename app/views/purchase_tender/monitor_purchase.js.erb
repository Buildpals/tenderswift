// TODO: Show some form of progress bar
setTimeout(function checkOnPayment() {
    $.get('<%= monitor_purchase_path(@purchase.request_for_tender) %>')
        .done(function (data) {
            console.log(data)
        });
}, 5000)