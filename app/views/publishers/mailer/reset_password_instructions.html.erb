<% if @resource.password.nil? %>
  <p>Hello <%= @resource.email %>!</p>

  <p>You are yet to set a password for your account. You can set your
    password through the link below</p>

  <p><%= link_to 'Set my password', edit_password_url(@resource,
                                                    reset_password_token: @token) %></p>

  <p>You can't set a password until you access the link above and set one</p>

  <p>If you didn't create an account at <a href="https://wwww.tenderswift
    .com">TenderSwift</a> please ignore this email .</p>

<% else %>

  <p>Hello <%= @resource.email %>!</p>

  <p>Someone has requested a link to change your password. You can do this through
    the link below.</p>

  <p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

  <p>If you didn't request this, please ignore this email.</p>
  <p>Your password won't change until you access the link above and create a new
    one.</p>

<% end %>
