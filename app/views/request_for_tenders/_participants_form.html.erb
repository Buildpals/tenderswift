<div class="nested-forms">
  <%= form.fields_for :participants do |participant| %>
      <%= render 'participant_fields', f: participant %>
  <% end %>
  <li class="d-flex w-100 justify-content-between">
    <%= link_to_add_association 'Add participant', form, :participants, class: 'btn btn-sm btn-block btn-primary my-1' %>
  </li>
</div>

