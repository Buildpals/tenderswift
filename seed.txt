    $('.submit-your-bid').click( function (e) {
        var numberOfPages = excelData.SheetNames.length;

        for(i = 0; i < numberOfPages; i++){
            json = process_wb(excelData, i);
            json.forEach(function (row, index, array) {
               if(row.length >= 4 && !isNaN(row[quantityColumn])){ //if the row has four columns and the quantity column contains a number
                   console.log(row[rateColumn]);
                   console.log(row);
                   //console.log(typeof row[rateColumn]);
               }
            });
        }
    });